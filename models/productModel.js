import mongoose from "mongoose";

const products = new mongoose.Schema({
  NAME: {
    type: String
  },
  PRODUCTION_DATE: {
    type: String
  },
  PRODUCT: {
    type: String
  },
  PRODUCTION_DATE: {
    type: String
  },
})